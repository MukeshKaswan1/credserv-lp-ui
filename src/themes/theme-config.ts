import { ThemeConfig } from './types';

const themeConfig: ThemeConfig = {
  templateName: 'Materail UI',
  mode: 'light',
  contentWidth: 'boxed',
  routingLoader: true,
  menuTextTruncate: true,
  navigationSize: 260,
  responsiveFontSizes: true,
  disableRipple: false,
};

export default themeConfig;
